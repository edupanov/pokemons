import {makeStyles} from "@material-ui/core/styles";

export const useFilterPanelStyles = makeStyles({
    filterPanel: {
        height: 'fit-content',
        width: 'auto'
    },
    fixedFilterPanel: {
        height: 49,
        width: 'auto'
    }
})